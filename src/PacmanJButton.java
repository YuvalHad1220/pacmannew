import javax.swing.*;
import java.awt.*;

public class PacmanJButton extends JButton {

    public PacmanJButton(String text, Font font){
        super(text);
        setFont(font);
        setForeground(Color.WHITE);
        setBackground(Color.BLACK);

    }
}
